services:
  - type: web
    name: nl-sql-streamlit
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app/main.py
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PYTHON_VERSION
        value: 3.12.0
